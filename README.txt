Indicaciones:
1.- El proyecto sera desarrollado con pyhton
2.- Se debera crear un entorno virtual para instalar dependencias (python -m venv venv)
3.- Ejecutar el comando para instalar dependencias descritas en el archivo requirement.txt
    pip install -r requirements.txt
